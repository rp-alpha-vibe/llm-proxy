# mask_strategy выбирает renderer: competition — локальный токен <TYPE_n>, placeholder — [[PII:TYPE:n]].
systems:
  alfa_tester:
    enabled: true
    pii_types: all
    allow_demask: true
    mask_strategy: competition
  demo:
    enabled: true
    pii_types:
      - person
      - phone
      - email
      - card
    allow_demask: true
    mask_strategy: placeholder
